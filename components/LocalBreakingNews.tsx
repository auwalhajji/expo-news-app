import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Page = () => {
  return (
    <View>
      <Text>Local Breaking News Page</Text>
    </View>
  );
};

export default Page;

const styles = StyleSheet.create({});
